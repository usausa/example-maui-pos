namespace Pos.Server.Host.Components.Dialogs;

using Pos.Server.Host.Models.Forms;

public sealed partial class AccountPasswordDialog
{
    private static readonly AccountPasswordFormValidator Validator = new();
}
