namespace Pos.Server.Host.Components.Dialogs;

using Pos.Server.Host.Models.Forms;

public sealed partial class CustomerEditDialog
{
    private static readonly CustomerFormValidator Validator = new();
}
