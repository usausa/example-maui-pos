<MudChip T="string" Color="Value.Color" Size="Size.Small" Label="true" Icon="@Value.Icon" Class="ma-0">@Value.Text</MudChip>

@code {
    // ViewHelper のチップ (文言・色・アイコン)
    [Parameter]
    public (string Text, Color Color, string? Icon) Value { get; set; }
}
