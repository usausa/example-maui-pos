<MudChip T="string" Color="Value.Color" Size="Size.Small" Label="true" Class="ma-0">@Value.Text</MudChip>

@code {
    // ChipText の戻り値 (文言と色)
    [Parameter]
    public (string Text, Color Color) Value { get; set; }
}
